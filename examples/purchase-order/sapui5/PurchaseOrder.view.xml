<mvc:View
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core"
  xmlns:form="sap.ui.layout.form"
  xmlns="sap.m"
  controllerName="demo.controller.PurchaseOrder">
  <Page title="Purchase Order Entry">
    <content>
      <form:SimpleForm editable="true" layout="ResponsiveGridLayout">
        <Label text="PO Number" />
        <Input value="{po>/poNumber}" required="true" placeholder="4500001234" />

        <Label text="Supplier" />
        <Input value="{po>/supplier}" required="true" placeholder="Acme Supplies Ltd." />

        <Label text="Document Date" />
        <DatePicker value="{po>/docDate}" displayFormat="yyyy-MM-dd" />

        <Label text="Currency" />
        <Select selectedKey="{po>/currency}">
          <items>
            <core:Item key="EUR" text="EUR" />
            <core:Item key="USD" text="USD" />
            <core:Item key="GBP" text="GBP" />
          </items>
        </Select>

        <Label text="Total Amount" />
        <Input value="{po>/amount}" type="Number" />

        <Label text="Urgent" />
        <Switch state="{po>/urgent}" />

        <Label text="Notes" />
        <TextArea value="{po>/notes}" rows="3" />
      </form:SimpleForm>

      <Toolbar>
        <ToolbarSpacer />
        <Button text="Save" type="Emphasized" press=".onSave" />
        <Button text="Cancel" press=".onCancel" />
      </Toolbar>
    </content>
  </Page>
</mvc:View>

