{
  "generatedAt": "fixture",
  "schemaVersion": "1.0.0",
  "components": [
    {
      "schemaVersion": "1.0.0",
      "id": "sap.m.Button",
      "name": "Button",
      "source": { "system": "sapui5", "ref": "fixture:sap.m.Button" },
      "category": "form",
      "status": "stable",
      "description": "Triggers an action.",
      "intentTags": ["primary-action", "secondary-action", "destructive"],
      "props": [
        {
          "name": "text",
          "type": { "kind": "string" },
          "required": false,
          "default": "",
          "constraints": [],
          "description": "Button label."
        },
        {
          "name": "type",
          "type": { "kind": "enum", "enumValues": ["Default", "Emphasized", "Transparent", "Reject", "Accept"] },
          "required": false,
          "default": "Default",
          "constraints": [],
          "description": "Visual style preset."
        }
      ],
      "events": [
        {
          "name": "press",
          "since": "1.0",
          "parameters": [],
          "description": "Fired when the user activates the button."
        }
      ],
      "slots": [],
      "composition": {
        "allowedWith": ["sap.m.Toolbar", "sap.m.Dialog", "sap.m.OverflowToolbar"],
        "forbiddenWith": [],
        "notes": ["Use one emphasized (primary) action per surface when possible."]
      },
      "tokens": { "semantic": {}, "componentOverrides": {} },
      "a11y": {
        "role": "button",
        "nameFrom": ["text", "ariaLabelledBy"],
        "keyboard": ["Enter", "Space"],
        "notes": []
      },
      "examples": [
        {
          "id": "button-min",
          "title": "Primary action button",
          "kind": "minimal",
          "uiPlan": {
            "type": "sap.m.Button",
            "props": { "text": "Save", "type": "Emphasized" }
          },
          "whenToUse": ["Primary action in a form or dialog"],
          "whenNotToUse": ["When action is destructive without confirmation"]
        }
      ],
      "versioning": { "since": "1.0", "deprecatedSince": null, "ui5Version": null }
    }
  ]
}

